name: "Pull request flow for Golang project"
description: "This is my standard pipeline for pull request on Golang project"

inputs:
  appId:
    required: true
    description: ID of the Github app
  privateKey:
    required: true
    description: Private key of the Github app
  workingDirectory:
    required: false
    default: "."
    description: "Directory containing the Go project (passed to underlying actions)"

runs:
  using: "composite"
  steps:
    - name: Check code quality 🫆
      uses: tchoupinax/repo-config/shared-actions/golang/golangci@57ec976eb717ad59ad7803e8be94cacc85e0c443 # v0.9.0
      with:
        workingDirectory: ${{ inputs.workingDirectory }}

    - name: Check build success 🦫
      uses: tchoupinax/repo-config/shared-actions/golang/goreleaser-build-only@57ec976eb717ad59ad7803e8be94cacc85e0c443 # v0.9.0
      with:
        workingDirectory: ${{ inputs.workingDirectory }}

    - name: Autodetect labels 🔍
      uses: tchoupinax/repo-config/shared-actions/dx/automatically-apply-labels@57ec976eb717ad59ad7803e8be94cacc85e0c443 # v0.9.0
      with:
        app-id: ${{ inputs.appId }}
        private-key: ${{ inputs.privateKey }}

